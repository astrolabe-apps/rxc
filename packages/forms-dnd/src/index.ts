// Public API for @rxc/forms-dnd
//
// dnd-kit-powered renderers that augment `@rxc/forms`. Apps that don't
// need drag-and-drop should not import this package — its peer
// dependencies (`@dnd-kit/*`) are not pulled in by `@rxc/forms`.

export { SortableArrayRenderer } from "./SortableArray";
