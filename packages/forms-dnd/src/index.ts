// Public API for @rx-controls/forms-dnd
//
// dnd-kit-powered renderers that augment `@rx-controls/forms`. Apps that don't
// need drag-and-drop should not import this package — its peer
// dependencies (`@dnd-kit/*`) are not pulled in by `@rx-controls/forms`.

export { SortableArrayRenderer } from "./SortableArray";
