{
  "changes": [
    {
      "comment": "Fix RenderArrayElements giving its children callback no tracking scope. The callback ran inline in the helper's own render, where no useComponentTracking window is open, so ambient control reads inside it subscribed nothing and the element never re-rendered — legacy radio groups rendered the wrong checked state while the underlying control updated correctly. Each element now gets its own RenderControl scope, matching v4, which mapped every element through a keyed RenderControl.",
      "type": "patch",
      "packageName": "@react-typed-forms/core"
    }
  ],
  "packageName": "@react-typed-forms/core",
  "email": "doolse@gmail.com"
}
